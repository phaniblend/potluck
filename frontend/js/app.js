// Main application logic
console.log('Potluck App Starting...');

// API Base URL is defined in auth.js, so we'll use it from there
// const API_URL = 'http://localhost:5000/api'; // Commented out to avoid duplicate

// We'll add dashboard-specific code here later